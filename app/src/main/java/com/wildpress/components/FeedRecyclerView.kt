package com.wildpress.components

import com.wildpress.model.Post

class FeedRecyclerView (posts: ArrayList<Post>) {
//   : RecyclerView.Adapter<PostRecyclerView.PostViewHolder>()
    private val posts: ArrayList<Post> = posts;
}